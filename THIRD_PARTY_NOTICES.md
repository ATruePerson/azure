# Third-party notices

Azure uses the official `@modelcontextprotocol/sdk` package for its Azure
Search stdio server. Its license and dependency notices are distributed in
`node_modules` and the Bun lockfile generated from the package registry.

Azure does not bundle or depend on ACC, OpenCodex, or their credentials.
