[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "acc-eval"
version = "0.1.0"
description = "Dependency-free analysis and reporting for ACC model-routing runs"
requires-python = ">=3.11"

[project.scripts]
acc-eval = "acc_eval.cli:main"
