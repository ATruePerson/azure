{
  "$schema": "https://azure.codes/schema/azure.json",
  "iconPath": "assets/prod/logo.svg",
  "scripts": [
    {
      "name": "Setup Worktree",
      "command": "vp i && ln -sf $AZURE_PROJECT_ROOT/.env .env && ln -sf $AZURE_PROJECT_ROOT/infra/relay/.env infra/relay/.env && node apps/web/scripts/warm-dep-cache.ts",
      "icon": "configure",
      "runOnWorktreeCreate": true
    }
  ]
}
